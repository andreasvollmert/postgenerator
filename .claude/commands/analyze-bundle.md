---
description: Analyze the project bundle size and dependencies
---

Analyze the project's bundle and dependencies:

1. Check package.json for all dependencies
2. Look for large dependencies that might bloat the bundle
3. Check if there are duplicate dependencies
4. Suggest optimizations (code splitting, lazy loading, tree shaking opportunities)
5. Run the build and report on output file sizes
